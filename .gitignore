node_modules
.env
.env.local
.turbo
dist
.next
build
coverage/
*.log

# TypeScript incremental build cache
*.tsbuildinfo

# Claude Code local settings (per-machine, never merge)
.claude/

# Local-only docs & backups (never merge)
*.pdf
!.github/**/*.md
!apps/web/public/aide/*.pdf
backup.sql
ideatank.txt
/scripts/

# Specs et roadmap versionnées — les fichiers .md à la racine et docs/ sont publics.
# Exclure uniquement les notes personnelles si besoin (ex: NOTES.md).

# Versioned project documentation
!docs/
!docs/ci-cd.md

apps/web/src/lib/klx-import/samples/

# Playwright artifacts
test-results/
playwright-report/

# Sentry Config File
.env.sentry-build-plugin
bugs.txt
